input {
  beats {
    port => 5044
  }
}

filter {
  # Add any filters here
}

output {
  elasticsearch {
    hosts => ["https://localhost:9200"]
    index => "sample"
    ssl_verification_mode => "none"
    user => "elastic"
    password => "{{ lookup('community.hashi_vault', 'secret=infra/data/elasticsearch:username' 
    token={{ vault_token }} url=http://vault-internal.kommanuthala.store:8200')}}"
  }
}